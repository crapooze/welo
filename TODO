* Embeddings
  - merge the branch
  - use the complex serialization overhead only when needed loaded
* Observations
  - change relationships into something different
  - observatory class to generate observations/actor which is able to observe?
  - decent examples
* Proxies
  - a class like observations to manipulate distant
